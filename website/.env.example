# Desk app base URL (no trailing slash). Website only depends on this for vacancies.
VITE_API_BASE=http://localhost:3000

# Public website URL (no trailing slash). Used for Open Graph, canonical, sitemap.
VITE_SITE_URL=http://localhost:5173
